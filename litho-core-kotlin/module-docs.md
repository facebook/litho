# Module litho-core-kotlin

Litho's codegen-free Kotlin API, including `KComponent`, `Style`, and hooks.

# Package com.facebook.litho

Definitions for `KComponent`, `Style`, and built-in hooks like `useState`.
